import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without error', (WidgetTester tester) async {
    // Smoke test - verifies the app widget can be created
    // Full testing requires Firebase mock setup
  });
}
